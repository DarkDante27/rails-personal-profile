 <%= link_to page_path(project), class: 'index-card-link' do %>
  <div class="col d-flex align-content-center flex-wrap card-category home-card-light" style="background-image: url(<%= project.photos.first %>)">
    <h2 class="card-title"><%= project.title %></h2>
  </div>
<% end %>
